services:
  - type: web
    name: academiahelper
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        name: X-Robots-Tag
        value: all
    routes:
      - type: rewrite
        source: /*
        destination: /index.html